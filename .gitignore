node_modules/
.env
*.env
.dev.vars
# gmail-brief.json is committed so Cloudflare can serve it to the live app
scanned-orders.json
processed-drive-scans.json
cf-screenshot.png
*.log
.netlify
.claude/worktrees/
# local state from `wrangler pages dev`
.wrangler/
