{
  "name": "sts-deploy",
  "private": true,
  "description": "Cloudflare Pages Functions dependencies for STS Workflow (frontend stays dependency-free/no-build)",
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2"
  }
}
